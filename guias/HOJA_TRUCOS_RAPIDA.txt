╔════════════════════════════════════════════════════════════════════════════════╗
║                          HOJA DE TRUCOS RÁPIDA                                  ║
║                    Exportación y Uso del Modelo de ML                           ║
╚════════════════════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────────────────────────┐
│ PASO 1: EJECUTAR EL SCRIPT PRINCIPAL (10-20 MINUTOS)                            │
└──────────────────────────────────────────────────────────────────────────────────┘

En PowerShell o Terminal:
    cd "ruta/a/carpeta"
    python practica_coches_2.py

Espera a que termine. Verás:
    ✓ Carga y exploración de datos
    ✓ Entrenamiento de modelos
    ✓ Comparación de rendimiento
    ✓ Optimización de hiperparámetros
    ✓ Exportación de archivos

Resultado: Se crea carpeta "modelos_exportados/"

═════════════════════════════════════════════════════════════════════════════════

┌──────────────────────────────────────────────────────────────────────────────────┐
│ PASO 2: VERIFICAR QUE FUNCIONA (1 MINUTO)                                       │
└──────────────────────────────────────────────────────────────────────────────────┘

En PowerShell o Terminal:
    python test_modelo.py

Debe mostrar:
    ✓ Modelo cargado exitosamente
    ✓ Predicción 1: $12,456.75
    ✓ Predicción 2: $42,890.50
    ✓ PRUEBA COMPLETADA EXITOSAMENTE

Si hay error: Revisa el mensaje de error en rojo

═════════════════════════════════════════════════════════════════════════════════

┌──────────────────────────────────────────────────────────────────────────────────┐
│ PASO 3: USAR EN TU CÓDIGO (1 LÍNEA)                                            │
└──────────────────────────────────────────────────────────────────────────────────┘

import joblib
import pandas as pd

# Cargar el modelo (una sola vez al inicio)
modelo = joblib.load('modelos_exportados/modelo_final_random_forest_*.joblib')

# Hacer predicción
datos = pd.DataFrame([{
    'year': 2020,
    'mileage': 50000,
    'engineSize': 2.0,
    'transmission': 'Automatic',
    'fuelType': 'Petrol',
    'brand': 'BMW',
    'model': 'Series 5'
}])

precio = modelo.predict(datos)[0]

print(f"Precio: ${precio:,.2f}")  # Salida: Precio: $28,456.75

═════════════════════════════════════════════════════════════════════════════════

┌──────────────────────────────────────────────────────────────────────────────────┐
│ ARCHIVOS IMPORTANTES EN "modelos_exportados/"                                    │
└──────────────────────────────────────────────────────────────────────────────────┘

📌 ARCHIVO MÁS IMPORTANTE:
   modelo_final_random_forest_*.joblib
   └─ Úsalo en tu aplicación web
   └─ Contiene todo: preprocessor + modelo
   └─ Una sola línea para predicciones

📊 PARA ENTENDER EL MODELO:
   metadatos_modelo.json
   └─ Ver: R² score, RMSE, MAE
   └─ Ver: Timestamp de entrenamiento
   └─ Ver: Features utilizados

🔍 VALIDAR INPUTS DEL USUARIO:
   categorias_mapping.json
   └─ Ver qué valores son válidos
   └─ Usar para crear dropdowns
   └─ Prevenir errores

💡 REFERENCIA DE CÓDIGO:
   ejemplo_uso_modelo.py (dentro de modelos_exportados/)
   └─ Código listo para copiar-pegar

═════════════════════════════════════════════════════════════════════════════════

┌──────────────────────────────────────────────────────────────────────────────────┐
│ VERIFICACIÓN RÁPIDA: ¿Todo instalado?                                          │
└──────────────────────────────────────────────────────────────────────────────────┘

En Python:
    >>> import pandas as pd
    >>> import numpy as np
    >>> import sklearn
    >>> import joblib

Si dice "No module named...": Instala con:
    pip install pandas numpy scikit-learn joblib

═════════════════════════════════════════════════════════════════════════════════

┌──────────────────────────────────────────────────────────────────────────────────┐
│ SOLUCIÓN RÁPIDA DE PROBLEMAS                                                    │
└──────────────────────────────────────────────────────────────────────────────────┘

❌ Error: "No module named joblib"
   ✓ pip install joblib

❌ Error: "No such file or directory: 'merc.csv'"
   ✓ Verifica que merc.csv esté en la misma carpeta que practica_coches_2.py

❌ Error: "modelos_exportados/ not found"
   ✓ Asegúrate de haber ejecutado practica_coches_2.py hasta el final

❌ Predicción da resultado muy alto/bajo
   ✓ Verifica que los datos estén en rangos razonables
   ✓ Abre metadatos_modelo.json para ver rangos esperados

❌ Error en GridSearchCV (tarda mucho)
   ✓ Es normal, GridSearchCV es lento por validación cruzada
   ✓ Puedes interrumpir con Ctrl+C y usar modelo sin optimizar

═════════════════════════════════════════════════════════════════════════════════

┌──────────────────────────────────────────────────────────────────────────────────┐
│ CÓDIGO RÁPIDO: INTEGRACIÓN EN FLASK                                             │
└──────────────────────────────────────────────────────────────────────────────────┘

from flask import Flask, request, jsonify
import joblib
import pandas as pd
import json

app = Flask(__name__)

# Cargar modelo al iniciar
modelo = joblib.load('modelos_exportados/modelo_final_random_forest_*.joblib')

@app.route('/predict', methods=['POST'])
def predict():
    datos = request.json
    df = pd.DataFrame([datos])
    precio = float(modelo.predict(df)[0])
    return {'precio': precio, 'formato': f'${precio:,.2f}'}

if __name__ == '__main__':
    app.run()

═════════════════════════════════════════════════════════════════════════════════

┌──────────────────────────────────────────────────────────────────────────────────┐
│ CÓDIGO RÁPIDO: DJANGO                                                           │
└──────────────────────────────────────────────────────────────────────────────────┘

# En views.py
import joblib
import pandas as pd
from django.http import JsonResponse
from django.views.decorators.http import require_POST

modelo = joblib.load('modelos_exportados/modelo_final_random_forest_*.joblib')

@require_POST
def predict_price(request):
    datos = {
        'year': int(request.POST['year']),
        'mileage': int(request.POST['mileage']),
        'engineSize': float(request.POST['engineSize']),
        'transmission': request.POST['transmission'],
        'fuelType': request.POST['fuelType'],
        'brand': request.POST['brand'],
        'model': request.POST['model']
    }
    
    df = pd.DataFrame([datos])
    precio = float(modelo.predict(df)[0])
    
    return JsonResponse({
        'exito': True,
        'precio': precio,
        'formato': f'${precio:,.2f}'
    })

═════════════════════════════════════════════════════════════════════════════════

┌──────────────────────────────────────────────────────────────────────────────────┐
│ CÓDIGO RÁPIDO: PREDICCIÓN EN LOTE (Batch)                                      │
└──────────────────────────────────────────────────────────────────────────────────┘

import joblib
import pandas as pd

modelo = joblib.load('modelos_exportados/modelo_final_random_forest_*.joblib')

# Predicción para múltiples vehículos
datos = pd.DataFrame([
    {'year': 2020, 'mileage': 50000, 'engineSize': 2.0, 
     'transmission': 'Automatic', 'fuelType': 'Petrol', 'brand': 'BMW', 'model': 'Series 5'},
    {'year': 2018, 'mileage': 100000, 'engineSize': 1.6, 
     'transmission': 'Manual', 'fuelType': 'Petrol', 'brand': 'Toyota', 'model': 'Corolla'},
    {'year': 2019, 'mileage': 60000, 'engineSize': 2.2, 
     'transmission': 'Automatic', 'fuelType': 'Diesel', 'brand': 'Mercedes', 'model': 'C Class'},
])

predicciones = modelo.predict(datos)

for i, precio in enumerate(predicciones):
    print(f"Vehículo {i+1}: ${precio:,.2f}")

═════════════════════════════════════════════════════════════════════════════════

┌──────────────────────────────────────────────────────────────────────────────────┐
│ CÓDIGO RÁPIDO: CARGAR METADATOS                                                │
└──────────────────────────────────────────────────────────────────────────────────┘

import json

# Ver información del modelo entrenado
with open('modelos_exportados/metadatos_modelo.json') as f:
    meta = json.load(f)

print(f"Modelo: {meta['nombre_modelo']}")
print(f"R² Score: {meta['metricas']['r2_score']:.4f}")
print(f"RMSE: ${meta['metricas']['rmse']:,.2f}")

# Ver valores válidos para categorías
with open('modelos_exportados/categorias_mapping.json') as f:
    cats = json.load(f)

print(f"Transmisiones válidas: {cats['transmission']['clases']}")
print(f"Combustibles válidos: {cats['fuelType']['clases']}")

═════════════════════════════════════════════════════════════════════════════════

┌──────────────────────────────────────────────────────────────────────────────────┐
│ COMPARATIVA: Carga con joblib vs pickle                                         │
└──────────────────────────────────────────────────────────────────────────────────┘

CON JOBLIB (RECOMENDADO):
    import joblib
    modelo = joblib.load('modelo.joblib')  # Más rápido

CON PICKLE:
    import pickle
    with open('modelo.pkl', 'rb') as f:
        modelo = pickle.load(f)  # Más lento

USE JOBLIB → Es la opción estándar de sklearn

═════════════════════════════════════════════════════════════════════════════════

┌──────────────────────────────────────────────────────────────────────────────────┐
│ CHECKLIST ANTES DE USAR EN PRODUCCIÓN                                          │
└──────────────────────────────────────────────────────────────────────────────────┘

[ ] ✓ practica_coches_2.py ejecutado sin errores
[ ] ✓ Carpeta modelos_exportados/ creada
[ ] ✓ test_modelo.py corre correctamente
[ ] ✓ Predicciones parecen razonables
[ ] ✓ Verificaste metadatos_modelo.json
[ ] ✓ Documentaste qué modelo estás usando
[ ] ✓ Creaste backups de los archivos del modelo
[ ] ✓ Probaste en tu código antes de producción

═════════════════════════════════════════════════════════════════════════════════

┌──────────────────────────────────────────────────────────────────────────────────┐
│ MÉTRICAS CLAVE A ENTENDER                                                      │
└──────────────────────────────────────────────────────────────────────────────────┘

R² Score (Coeficiente de Determinación):
  • Rango: 0 a 1
  • Cercano a 1: Modelo excelente
  • Cercano a 0: Modelo malo
  • Interpretación: % de varianza explicada
  • Ejemplo: 0.8543 = Explica el 85.43% de la variación

RMSE (Raíz del Error Cuadrático Medio):
  • Rango: Positivo
  • Más bajo es mejor
  • Penaliza errores grandes
  • Unidades: Pesos (€, $, etc)
  • Ejemplo: $5,234.50 = Error típico de $5,234

MAE (Error Absoluto Medio):
  • Rango: Positivo
  • Más bajo es mejor
  • Más interpretable que RMSE
  • Unidades: Pesos (€, $, etc)
  • Ejemplo: $3,456.75 = Error promedio de $3,456

═════════════════════════════════════════════════════════════════════════════════

📚 PARA MÁS INFO, LEE:
   - RESUMEN_PARTES_4_Y_5.md (Explicación completa)
   - GUIA_EXPORTACION_MODELO.md (Guía detallada)
   - ESTRUCTURA_EXPORTACION.md (Diagramas)

🚀 ¡LISTO PARA USAR! 🚀

═════════════════════════════════════════════════════════════════════════════════
